{
  "name": "xai-vanguard-node",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "pnpm recursive run lint",
    "clean": "pnpm recursive run clean",
    "build": "pnpm recursive run build",
    "cli": "pnpm -filter @xai-vanguard-node/core run build && pnpm -r -filter @xai-vanguard-node/vanguard-node-cli run start",
    "desktop": "pnpm -filter @xai-vanguard-node/web-connect run build && pnpm -r -filter @xai-vanguard-node/vanguard-client-desktop run dev",
    "web": "pnpm -filter @xai-vanguard-node/web-connect run dev",
    "docs": "pnpm -filter @xai-vanguard-node/docs run start",
    "deploy-smart-contracts": "pnpm -r -filter @xai-vanguard-node/core run build && pnpm -r -filter @xai-vanguard-node/smart-contracts run deploy"
  },
  "keywords": [],
  "author": "",
  "license": "UNLICENSED",
  "dependencies": {
    "ethers": "^6.7.1"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.6.0",
    "@typescript-eslint/parser": "^6.6.0",
    "concurrently": "8.2.1",
    "copyfiles": "2.4.1",
    "eslint": "^8.48.0",
    "eslint-plugin-only-warn": "^1.1.0",
    "rimraf": "5.0.1",
    "typescript": "^5.2.2"
  }
}
