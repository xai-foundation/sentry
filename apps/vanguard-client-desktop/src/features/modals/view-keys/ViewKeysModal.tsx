export function ViewKeysModal() {
	return (
		<div className="fixed right-0 top-0 w-full h-auto bg-white">

			<div className="absolute bottom-0 left-0 w-full p-6">
				<button
					// onClick={() => {
					// 	setShowModal(false);
					// 	setNumber(number + 1);
					// }}
					className="w-full h-16 bg-[#F30919] text-sm text-white p-2 uppercase font-semibold"
				>
					Continue
				</button>
			</div>
		</div>
	)
}
