{
  "name": "@sentry/cli",
    "targets": {
      "build": {
        "dependsOn": [
          {
            "target": "build",
            "projects": "dependencies"
          }
        ],
        "outputs": [
          "{projectRoot}/dist",
          "{projectRoot}/release", 
          "{projectRoot}/build",
          "{projectRoot}/tsconfig.tsbuildinfo"
        ]
    }
  }
}